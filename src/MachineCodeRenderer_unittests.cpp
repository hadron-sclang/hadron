#include "hadron/MachineCodeRenderer.hpp"

#include "doctest/doctest.h"

namespace hadron {

// Maybe useful to use an assembler for virtual asm? What would be cool then is to take an input string in virtual
// asm, assemble, then can run through the renderer, print the output and compare?
/*
TEST_CASE() {
    SUBCASE() {

    }
}
*/
} // namespace hadron